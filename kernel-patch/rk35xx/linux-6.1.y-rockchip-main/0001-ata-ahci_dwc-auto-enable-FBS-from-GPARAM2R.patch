From 0000000000000000000000000000000000000001 Mon Sep 17 00:00:00 2001
From: Community Fix <fix@rk3568-nas.local>
Date: Sun, 19 Apr 2026 00:00:00 +0800
Subject: [PATCH] ata: ahci_dwc: 由硬件综合参数自动启用 FIS-Based Switching

DWC AHCI IP 在 GPARAM2R 寄存器的 FBS_SUP 位（bit 15）报告该控制器
在芯片综合时是否内置了 FBS 支持。ahci_dwc_check_cap() 已读取此位，
但未将其传递给 libahci。

在 fbs_sup 为真时设置 AHCI_HFLAG_YES_FBS，使 libahci 在
ahci_platform_init_host() 的 ahci_save_initial_config() 中将端口
标记为 FBS 可用，无需对每款 SoC 单独列举 compatible。

Signed-off-by: Community Fix <fix@rk3568-nas.local>
---
 drivers/ata/ahci_dwc.c | 3 +++
 1 file changed, 3 insertions(+)

diff --git a/drivers/ata/ahci_dwc.c b/drivers/ata/ahci_dwc.c
--- a/drivers/ata/ahci_dwc.c
+++ b/drivers/ata/ahci_dwc.c
@@ -1,6 +1,9 @@
 	if (fbs_sup && fbs_pmp < AHCI_DWC_FBS_PMPN_MAX) {
 		dev_warn(&dpriv->pdev->dev, "PMPn is limited up to %u ports\n",
 			 fbs_pmp);
 	}
 
+	if (fbs_sup)
+		hpriv->flags |= AHCI_HFLAG_YES_FBS;
+
 	for_each_set_bit(i, &port_map, AHCI_MAX_PORTS) {
